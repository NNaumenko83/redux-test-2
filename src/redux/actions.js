// import { nanoid } from "nanoid";
// import { createAction } from "@reduxjs/toolkit";

// export const addTask = createAction("task/addTask", (text) => {
//   return { payload: { id: nanoid(), task: text, complete: false } };
// });

// console.log(addTask("Hello"));

// // export const addTask = (text) => ({
// //   type: "task/addTask",
// //   payload: { id: nanoid(), task: text, complete: false },
// // });

// export const deleteTask = createAction("task/deleteTask");
// console.log(deleteTask.type);

// // export const deleteTask = (id) => ({
// //   type: "task/deleteTask",
// //   payload: id,
// // });
